# .env.example
# Copy this file to .env and replace placeholders with your real values.
# Do not commit real secrets to source control.

# Email (SMTP) — e.g., Gmail address
GMAIL_USER=your-email@example.com
# App password (Gmail 2FA app password) or SMTP password. Get from "https://myaccount.google.com/apppasswords"
GMAIL_APP_PASS=your_app_password_here

# Server port
PORT=5000

# Redis config
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=your_redis_password

# Cloud drive IDs
ONEDRIVE_SHAREID=your_onedrive_share_id
GDRIVE_FOLDERID=your_gdrive_folder_id

# JWT secret (keep secure)
JWT_SECRET=your_jwt_secret

# Google API key for GDrive
GDRIVE_API_KEY=YOUR_GOOGLE_API_KEY_HERE

# Firebase service account:
# Recommended: point to a file instead of embedding JSON (sa JSON often contains newlines)
FIREBASE_ACCOUNT_KEY_PATH=/path/to/firebase-service-account.json

# Alternative (embedded JSON). If used, quote and escape newlines:
# FIREBASE_ACCOUNT_KEY_JSON='{"type":"service_account","project_id":"project-id","private_key_id":"KEY_ID","private_key":"-----BEGIN PRIVATE KEY-----\nYOUR_PRIVATE_KEY_CONTENT\n-----END PRIVATE KEY-----\n","client_email":"service-account@example.iam.gserviceaccount.com","client_id":"client-id"}'